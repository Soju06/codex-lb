The hosted unit gate on f85d098f89ffe8fddd3b53f3139469f37f871006 exposed an integration omission: production's tool-call field allowlists include `async`, whereas the fixture rebuild rules inherited from main do not. Keep the existing scalar rule used for protocol flags; the sanitizer preserves safe scalar shapes and production remains responsible for validating marker semantics. For example, a custom call with `async: true` keeps that flag while its captured input and output are replaced. No existing production validation is relaxed.
