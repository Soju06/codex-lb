## 1. Close the backend area gap

- [x] 1.1 Add a failing regression asserting the native packaging paths match the `backend` filter.
- [x] 1.2 Add `Cargo.toml`, `Cargo.lock`, `crates/**`, `Dockerfile` and `Dockerfile.*` to the `backend` filter.
- [x] 1.3 Record the requirement in the `github-automation` capability.
- [x] 1.4 Run the repository gates and confirm the unit slice covers the new paths.
