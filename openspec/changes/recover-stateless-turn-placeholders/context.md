The incident is an HTTP 502 with `previous_response_owner_unavailable`. The proxy issues synthetic turn-state headers during WebSocket acceptance before account selection completes. A later HTTP fallback can therefore carry an unregistered marker without ever having established account-owned upstream state. A marker's shape alone cannot prove a continuation. The complete-request validator distinguishes the safe bounded case; missing ownership for opaque history remains unresolved.
