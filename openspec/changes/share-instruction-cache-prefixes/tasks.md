# Tasks

- [x] 1. Add the default-on application setting.
- [x] 2. Derive a model-specific wire cache key from the exact stable prefix.
- [x] 3. Add the explicit breakpoint without mutating client-visible identity.
- [x] 4. Cover supported models, prefix changes, existing breakpoints, and
      unsupported request shapes with focused tests.
- [x] 5. Validate OpenSpec, run focused tests, lint, and type checks.
