The previous task failed at 2026-09-11 23:01:35 Asia/Shanghai with `trae_unsupported_request: Unsupported TRAE input item: compaction` before upstream inference (0 ms). This is request compatibility, not a failing lightweight health probe. Source-level busy responses were also present during concurrent requests. Do not silently drop compressed history or increase concurrency as a workaround.

The scheduler only persists completed probes, so report fresh completed counts, not a fabricated running/progress state. Admission counters are replica-local; durable probe results are shared. Errors are shown by code, not arbitrary upstream messages which may contain confidential request data. Read-only preview uses an isolated database snapshot and disables background scheduling; it is not evidence of current production in-flight counts.
