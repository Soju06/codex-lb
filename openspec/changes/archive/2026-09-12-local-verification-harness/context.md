The September 12 compaction rollout initially tried newer repository fixtures against an older deployed application and launched a probe while readiness was still pending. Dated scripts also printed only their final result. The reusable harness addresses these specific failures without automating restart or route mutation.

Stages own deadlines and cleanup. Synchronous database checks run as owned process groups so they cannot block progress indefinitely. HTTP parsing accepts fragmented UTF-8, CRLF and multiline data frames with a finite frame limit. The first valid completed terminal is authoritative; heartbeats and headers alone never pass. WebSocket failures and incomplete terminals fail immediately. Reports never copy raw provider error messages or opaque state.

Release testing resolves the fixture Git revision before any write, puts test files in a temporary tree and loads only the selected release's application package. It records fixture, application and test-source provenance, uses temporary SQLite settings, disables ambient pytest plugin autoload and kills its process group on timeout/cancellation. Private logs are linked from partial reports.

Verification on 2026-09-12: 36 harness/database-gate/stable-entry checks pass, including direct legacy script invocation from another working directory. The selected deployed release passes 37 regressions in the isolated runner with fixture revision b54696466d8e073832b37dd0655a14ca776fc00b. Its application Python digest is unchanged after tests. Real PostgreSQL compatibility, both readiness gates, HTTP, WebSocket, compaction on 2461 and summary recompaction/replay on 2462 all pass, with the final overall status recorded as passed. Two read-only idle samples of 2461 also pass. No service restart, drain or routing change was performed.

Ruff and type checks pass. Global OpenSpec validation remains 49 passed / 17 failed, matching the pre-existing baseline; this change is strictly valid. Final evidence is in the local managed-bots artifacts/verification-harness-20260912 directory.
