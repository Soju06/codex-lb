# Local verification, 2026-09-12

- PostgreSQL 18 Alpine staged on loopback 55432 with a dedicated Docker volume. Docker Hub timed out; the existing mirror path supplied the image pinned by content digest. Credentials are private local files, outside Git.
- Exact production release schema (`20260910_190000_company_source_budget`) was independently migrated into PostgreSQL. No production schema revision was changed.
- Rehearsal snapshot: 59 tables, 107,022 rows; canonical row counts and hashes matched before commit. Migration-seeded singleton rows in the unused target were inspected and cleared before import.
- Two backends on 2461/2462 concurrently wrote dashboard settings ten times and passed actual HTTP/SSE and WebSocket TRAE Luna calls. Initial upstream response-header timeout was rejected; subsequent acceptance succeeded.
- Isolated entrance validated A→B→A. Original WebSocket completed two turns after switch, new connection stayed on B through rollback, and all connections drained. An extra operator query initially used the wrong ring metadata column; corrected readback confirmed distinct advertised loopback endpoints. This did not affect the successful acceptance harness.
- User authorized the initial maintenance window. The old SQLite LaunchAgent was disabled and stopped; acquisition of its exclusive lifetime lock confirmed the writer had exited before taking the final snapshot.
- Final import: 59 tables, 107,222 rows, all canonical checksums matched. Original SQLite, encryption key, client configuration checksum and launch configuration are preserved privately under `~/.codex-lb/postgres/cutover`.
- Both production LaunchAgents now share `codex_lb` on PostgreSQL. The 2457 entrance points to blue (2461); green (2462) remains ready. The 2455 compatibility entrance forwards to 2457. Original SQLite startup is disabled, preventing accidental parallel divergent data stores.
- Final pair A→B→A and retained WebSocket acceptance passed, then the production entrance performed its own validated switch. Compatibility-port HTTP/SSE and WebSocket acceptance also passed. Twelve recent model probes were successful. One scheduler leader and both distinct ring endpoints were verified.
- Stop-to-validated-entry-switch took 178 seconds. LaunchAgent cold imports took longer than the foreground rehearsal and exceeded the first 60-second readiness poll; no unhealthy backend was admitted. Future rehearsals should use the same LaunchAgent environment and wait for readiness before starting acceptance; a startup timeout must not be followed by unconditional inference probes.
- A PostgreSQL custom-format backup was restored into an isolated database with `pg_restore --exit-on-error`; all 59 tables and the original revision were present, including six accounts and three model sources. Codex config's SHA-256 is unchanged and its endpoint remains commented out.
- Validation: 14 focused tests passed against a dedicated real PostgreSQL test database; Ruff and ty passed. OpenSpec 1.13 validated the change, the owning capability and all 66 repository specs. The older locally cached OpenSpec 1.3 rejected existing specs; validation was repeated with the current cached 1.13 CLI.
