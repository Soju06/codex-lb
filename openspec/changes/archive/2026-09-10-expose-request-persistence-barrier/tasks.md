## 1. Reproduction and contract

- [x] 1.1 Reproduce held real HTTP settlement on pinned main and record current counters, reservation and canonical drain result.
- [x] 1.2 File focused issue2343 and record the accepted additive contract before behavior edits.
- [x] 1.3 Capture failing public status and completed-task callback handoff regressions.

## 2. Implementation and proof

- [x] 2.1 Expose canonical registered persistence ownership and verify callback, zero, fallback and cancellation controls.
- [x] 2.2 Map reliable observation to pending/drained and invalid/unsupported observation to unknown; verify nonblocking loopback status behavior.
- [x] 2.3 Verify full native HTTP settlement, active/idle bridge behavior, reversible drain, admission rejection and exactly-once persistence.
- [x] 2.4 Pass required checks and strict OpenSpec, then freeze independent review tied to exact candidate/base.

## 3. Delivery

- [x] 3.1 Sync verified requirements/context and archive after exact independent review; retain the verification record for the issue-linked PR.
