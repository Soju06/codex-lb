# Verification

Source base c52941a4ca66a62a85b1a68e0905296f0546a54d; issue2343. The preserved diagnostic reproduces real native HTTP detached settlement while global/bridge counters read zero. The new status assertion fails on base. Four callback negative controls fail when the observer filters completed tasks, then pass with registered ownership retained.

The synchronous observer reuses the existing shutdown classifier and sets, includes done-but-registered owners and leaves settlement/bridge execution unchanged. Loopback response mapping rejects missing, throwing and invalid observation as unknown with no count. The full native route verifies active work, late admission rejection, pending reserved settlement after response completion, repeated polling, reversible stop and exactly-once logs/reservations.

114 focused health/drain and active/idle WebSocket controls pass;42 existing settlement controls pass. The dedicated new suite passes15 cases after fixture typing and deterministic response completion. Required lint/format, four architecture checks and ty pass. Strict change validation passes; all65 canonical specs pass after sync. No migration, setting, default, dependency or frontend changes. Tests use identical disposable database environment variables with main/background/fixture routing verified before launch. Independent ERR Standards/Input review of1166252d found no production or input findings; its sole P4 docs gap is addressed in docs/deployment/docker.md. Fifty additional persistence/lifecycle controls pass. Publication and live acceptance remain separate.
