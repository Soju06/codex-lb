## 1. Reproduce and correct

- [x] 1.1 Reproduce native stateless placeholder HTTP 502 on integrated source and compare current main using isolated database engines.
- [x] 1.2 Add the failing HTTP route case to the PR head and allow only complete account-neutral placeholder requests; verify red then green.
- [x] 1.3 Add the equivalent WebSocket route regression and apply the same admission rule; verify red then green.
- [x] 1.4 Verify complete tool transcripts and rejection of opaque, unknown, incomplete and previous-response continuations, plus registered/file owner constraints through public routes.

- [x] 1.5 Reproduce the raw HTTP fallback through both public routes and apply the same whole-body predicate; both red cases now pass.

## 2. Verify and deliver

- [x] 2.1 Run relevant route, ownership, replay and static checks with both database URLs isolated; retain exact candidate evidence.
- [x] 2.2 Validate and synchronize the specification, independently review the exact correction, and provide an immutable integrated correction to the deployment owner.
- [ ] 2.3 Publish the existing issue-linked PR update and transfer hosted monitoring ownership with current-head evidence.
