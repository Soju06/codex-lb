# Filter company model catalog by observed availability

Company models are advertised without independent evidence that they are responsive. Run a minimal, leader-only inference health check for every enabled company model every two hours, then filter client catalogs using those durable per-model results and shared source admission state. Keep configured models visible to operators and callable explicitly. User traffic and request-specific validation failures do not control catalog visibility.
