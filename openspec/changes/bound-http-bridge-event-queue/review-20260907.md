# September 7 integration review

Candidate `103f624c5b511f0732fc0122cde44be3cb86ff53`, tree
`301e91a2a349a98e3c53c30111a42274064dd14c`, against base
`35ccf8e9369b10e5819229ac674a3e1c5f6569ae`. The previous accepted implementation
boundary was `5ad638b6..3bfe0f8d`. Two independent read-only reviewers examined
the incremental timing integration. Neither ran tests or inspected live data.
This record does not cover subsequent source changes.

## Standards

No explicit Standards-axis violation found in the incremental integration.

- Timing ownership. The proxy architecture fitness gate requires explicit
  collaborators and forbids increasing raw-timing allowances. Queue
  construction, enqueue/deferred enqueue, and read helpers propagate
  scheduler/clock. Four required-keyword entries are added without allowance
  increases.
- Read semantics. The change's proxy-architecture delta requires same-task
  timeout behavior, including nonpositive deadlines. `Scheduler.timeout`, its
  real alias, and the virtual adapter preserve that interface. New simulation
  coverage addresses expiry, cancellation, retained payloads, and delayed
  terminals.
- Scope and simplicity. CONTRIBUTING's collaborator rules permit large
  single-capability changes. This remains one bridge-buffering concern despite
  its size. No new setting, required setup, dashboard surface, or README section
  triggers PRINCIPLES P1-P5 exceptions.
- OpenSpec. Proposal, design, tasks, and normative deltas are present. Integration
  verification/delivery tasks remain unchecked at this review boundary. The
  change is not falsely archived.

Judgment, not an explicit violation: queue internals remain substantial within
`request_submit.py`. Extraction could improve cohesion, but the integration
does not introduce a new responsibility beyond the preserved implementation.
Helper-level simulation tests supplement existing product-path regressions.

Readiness remains unproven, not failed. CONTRIBUTING requires current-head
hosted CI, CodeRabbit handling, and clean mergeability. The candidate was
unpushed at review. The prescribed full local gate remains subject to the
task's explicit browser/container safety exclusion. Task 7.7 still requires
maintainer acceptance of the native fallback and residual performance cost.

## Input

No actionable implementation finding for the pinned candidate. The integration
preserves both accepted intents.

- Same-task reads remain intact at `streaming.py:282`. The scheduler timeout
  preserves prior nonpositive-timeout behavior. The new simulation file checks
  real/virtual raced publication without child tasks, late-event retention,
  external cancellation, selected terminal plus EOS, and timer/task quiescence.
- Producer cancellation still defers through child reaping and reservation
  release at `request_submit.py:568`. Only task/wait ownership changed.
- `test_http_bridge_reader_deadline.py:103` observes the actual shared reader
  delivering the sibling's `response.failed/request_timeout` plus EOS after the
  paused request's own deadline, with pending state cleared and virtual tasks
  and timers settled.
- Delayed attachment remains set before submit, cooldown, and registration.
  The six route cases still observe the selected liveness failure after sibling
  finalization and restore byte credits.
- Timing-guard requirements cover all four queue helpers. Native fallback
  remains confined to bridge socket creation and reconnect.

Delivery evidence was incomplete at review. The older `performance.md` records
historical revisions, and current integration verification, benchmarking,
lease-push, and hosted gates were pending. Record current-candidate results
before claiming completion. This is unfinished delivery evidence, not a
demonstrated behavioral defect.

Standards: 0 explicit violations. Input: 0 actionable implementation findings.
Maintainer acceptance and exact-head hosted delivery evidence remain separate.
