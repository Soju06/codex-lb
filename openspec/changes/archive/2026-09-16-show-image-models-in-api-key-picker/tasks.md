## 1. Model catalog

- [x] 1.1 Share the Images model allowlist with the typed dashboard listing and add deduplicated image entries independently of registry state.
- [x] 1.2 Add route-level regressions for bootstrap, refreshed/empty catalogs, collisions and saved image-model allowlists; preserve public proxy catalogs.

## 2. Dashboard verification

- [x] 2.0 Mark adapter entries as image-only and keep them out of the shared Automations model picker with a product-flow regression.
- [x] 2.1 Add and run a create/edit API-key product-flow regression for selecting and persisting image models.
- [x] 2.2 Capture and inspect before/after image-model picker screenshots using local fixtures.

## 3. Completion

- [x] 3.1 Run relevant backend/frontend tests, lint/type checks and strict OpenSpec validation; record verification evidence.
- [x] 3.2 Sync delta requirements and stable context into main specs, then archive the verified change.
