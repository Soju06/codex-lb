## 1. Backend

- [x] 1.1 Persist and expose optional independent caps; verify API validation, removal, defaults, and migration round trip.
- [x] 1.2 Enforce caps in selection and connection reuse without changing ownership or stored status; verify threshold, reset, weekly-only, disabled, and pinned-request regression tests.

## 2. Frontend

- [x] 2.1 Add Accounts-page controls and individual quota-bar markers; verify interaction, read-only, disabled-window, and marker-position tests.

## 3. Verification

- [x] 3.1 Run focused backend/frontend suites, lint, typecheck, and strict OpenSpec validation; synchronize specs and archive only after verification. Scope checks passed; baseline-wide OpenSpec issues are recorded in verification.md.
