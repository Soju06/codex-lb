## 1. Source alias contract

- [x] 1.1 Validate and resolve optional upstream IDs without changing catalog identity or source selection.
- [x] 1.2 Map supported HTTP requests and structured JSON/SSE response model fields with cleanup preserved.

## 2. Dashboard workflow

- [x] 2.1 Support validated alias entries in create/edit forms and preserve existing capabilities during rename.
- [x] 2.2 Add localized help and form regression coverage.

## 3. Verification

- [x] 3.1 Exercise catalog, authorization, accounting, HTTP routes and fragmented stream behavior with local upstream stubs.
- [x] 3.2 Run relevant backend/frontend checks and strict OpenSpec validation; record results.
- [x] 3.3 Sync stable specs/context and archive only after verification.
