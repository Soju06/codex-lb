Health uses completed request logs, shared by Responses and Chat. No periodic inference consumes tokens. Recovery is passive after a fixed 60-second cooldown; existing per-source concurrency limits remain applicable to Responses. Statistics cover a rolling 24 hours. Budgets use observed input plus output tokens (cached input is already included), are soft admission limits, and cannot substitute for provider quota. No automatic provider or subscription fallback is added.
