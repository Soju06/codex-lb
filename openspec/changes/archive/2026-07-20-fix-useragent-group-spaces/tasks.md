## 1. TDD unit: parser regression and fix

- [x] 1.1 In `tests/unit/test_proxy_utils.py`, add exactly one test node named `test_request_log_useragent_fields_preserves_codex_desktop_product_name` for `Codex Desktop/0.142.4 (Mac OS 26.5.2; arm64) unknown (Codex Desktop; 26.623.70822)`, asserting the stripped stored user-agent and `useragent_group == "Codex Desktop"`.
- [x] 1.2 Run the new test RED before changing production code: `uv run pytest -q tests/unit/test_proxy_utils.py::test_request_log_useragent_fields_preserves_codex_desktop_product_name`.
- [x] 1.3 Make the minimal GREEN change in `app/modules/proxy/_service/support.py::_request_log_useragent_fields`: retain stripping and blank-to-`(None, None)` handling, replace whitespace-token truncation with the content before the first `/`, and use the entire stripped value when no slash exists. Keep the shared helper as the source for both HTTP and WebSocket Responses logging.
- [x] 1.4 Run the parser GREEN check and existing focused null/case coverage: `uv run pytest -q tests/unit/test_proxy_utils.py::test_request_log_useragent_fields_preserves_codex_desktop_product_name tests/unit/test_proxy_utils.py::test_request_log_useragent_fields_accept_lowercase_header_name tests/unit/test_request_log_useragent_fields_handle_missing_and_blank_headers`.
- [x] 1.5 Run the discoverable request-log persistence check and WebSocket Responses suite: `uv run pytest -q tests/integration/test_request_logs_api.py::test_request_logs_api_returns_useragent_fields tests/integration/test_proxy_websocket_responses.py`.

## 2. TDD unit: migration regression, revision, and validation

- [x] 2.1 In `tests/integration/test_migrations.py`, add the SQLite regression node `test_request_log_useragent_group_migration_normalizes_existing_rows` with nonblank slash-delimited, Codex Desktop, slashless, blank, and null user-agent rows; assert canonical groups, blank/null `NULL`, idempotent reapplication, and unchanged data after downgrade.
- [x] 2.2 Run the migration regression RED before adding the revision: `uv run pytest -q tests/integration/test_migrations.py::test_request_log_useragent_group_migration_normalizes_existing_rows`.
- [x] 2.3 Make the minimal GREEN migration change in `app/db/alembic/versions/20260720_000000_normalize_request_log_useragent_groups.py`, parented to `20260717_000000_optimize_dashboard_hot_path_indexes`: use set-based SQL branches appropriate to SQLite and PostgreSQL to derive groups from stripped stored user-agents, and make `downgrade()` a no-op.
- [x] 2.4 Rerun `uv run pytest -q tests/integration/test_migrations.py::test_request_log_useragent_group_migration_normalizes_existing_rows` GREEN, then run the focused migration policy test `uv run pytest -q tests/unit/test_db_migrate.py::test_check_migration_policy_reports_head_and_format_violations`.
- [x] 2.5 Run required SQLite migration/head validation with `make migration-check`; when the project provides a PostgreSQL test database, also run `make migration-check-postgres` and `uv run pytest -q tests/integration/test_migrations.py::test_postgresql_migration_contract_policy_and_drift_match tests/integration/test_migrations.py::test_postgresql_upgrade_head_from_empty_database`. If PostgreSQL is unavailable, retain the SQLite regression result and record PostgreSQL validation as unavailable rather than claiming PostgreSQL tests passed.
- [x] 2.6 Run final focused verification: `openspec validate fix-useragent-group-spaces --strict`, `make migration-check`, and confirm the migration check reports one linear Alembic head with no policy or drift violations.
