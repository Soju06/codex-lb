## 1. CLI repairs

- [x] 1.1 Reproduce credential argument exposure with synthetic input; replace URL arguments with explicit environment selection and verify subprocess success and missing-input rejection.
- [x] 1.2 Reproduce missing per-step stamps in successful and failed CLI reports; retain each attempt's stamps and pass both regressions.

## 2. Verification

- [x] 2.1 Update usage and sync requirements; pass strict OpenSpec validation.
- [x] 2.2 Pass affected tests and repository checks; obtain independent exact-candidate review.
- [x] 2.3 Verify requirement coverage and archive readiness against the passing tests and independent review. Track publication and hosted checks separately in the delivery receipt.
