apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: codex-lb-metrics
spec:
  selector:
    matchLabels:
      app: codex-lb
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
